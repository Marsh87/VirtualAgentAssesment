Steps to Run
1. Run in bootstrap.sql script in Scripts folder in sql server
2. Run in InteractiveMVC_Tables_With_Data.sql in the Scripts folder against VirtualAgent database created in step 1
4. Restore Nuget Packages
5. Change Connection string in Web.config if needed,you probably need to the change the data source if not using the localhost server
6. Make sure culture setting on environment for numbers is set to "."
7. Debug new instance VirtualAgentAssessment